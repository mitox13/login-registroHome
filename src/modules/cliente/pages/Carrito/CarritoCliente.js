import React from 'react'
// import AuthContext from '../../../../context/authContext'

const CarritoCliente = () => {

    // const { carrito } = useContext(AuthContext)
    // console.log(carrito);
    return (
        
        // carrito[0]?
        
        <div className='text-white'>
            <h1>CARRITO CARRITO</h1>
            
                {/* {
                    
                    carrito[0].map((carritoOferta, i) => (
                        <ul key={i}>
                            <li >{carritoOferta.diaDeOferta}</li>
                            <li >{carritoOferta.horaDeInicio}</li>
                            <li >{carritoOferta.costo}</li>
                        </ul>
                    ))
                } */}
            

        </div> 
        // : null
    )
}

export default CarritoCliente
